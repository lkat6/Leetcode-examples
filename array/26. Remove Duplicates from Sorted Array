class Solution:
    def removeDuplicates(self, nums: List[int]) -> int:
        n = 0
        s = []
        while n < len(nums):
            if nums[n] in s:
                nums.remove(nums[n])


            else:
                s.append(nums[n])
                n +=1
